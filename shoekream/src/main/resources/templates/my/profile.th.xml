<thlogic>
    <attr sel="#profile_img" th:src="${profile.imgUrl == ''} ? '/img/profile.png' : ${profile.imgUrl}"/>
    <attr sel="#profile_name" th:text="${profile.nickname}"/>
    <attr sel="#profile_email" th:text="${profile.email}"/>
    <attr sel="#change_email" th:placeholder="${profile.email}"/>
    <attr sel="#profile_password" th:text="${profile.memberPw}"/>
    <attr sel="#profile_nickname" th:text="${profile.nickname}"/>
    <attr sel="#profile_hp" th:text="${profile.hp}"/>
    <attr sel="#profile_shoe" th:text="${profile.shoeSize}"/>
</thlogic>